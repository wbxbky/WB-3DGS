experiment: supplementary_s6_partition_sensitivity
base_config: ../paper_defaults.yaml
protocols: [interleaved_1_in_8, revised_blocked_split]
wind_regimes: [L1, L2, L3]
metrics: [PSNR, SSIM, LPIPS]
paired_bootstrap: {unit: sequence, resamples: 10000, seed: 42, ci_percent: 95}

